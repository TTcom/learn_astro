// import macomponents from 'mariocomponents'
// import component from 'nodelist'

export default (app) => {
//   console.log('Why is initializing one time per component!', component)
//   app.use(macomponents).use(component)
}
